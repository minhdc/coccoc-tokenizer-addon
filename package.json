{
  "name": "coccoc-tokenizer-addon",
  "version": "1.0.0",
  "description": "wrapper for coccoc-tokenizer-cpp",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-gyp": "^6.0.1"
  },
  "dependencies": {
    "node-addon-api": "^2.0.0"
  }
}
